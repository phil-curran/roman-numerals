let number = 9;

for (let i = number; i > 0; i--) {
  if (number <= 4){
    romanNumeral.push("I");
  } else if (number )
}

const symbols = {
  'I': 1,
  'V': 5,
  'X': 10,
  'L': 50,
  'C': 100,
  'D': 500,
  'M': 1000
};

if (number < reducer) {
  numberLadder(number);
} else {
  romanNumeral.push(numeral);
}
// console.log(number, numeral, reducer);